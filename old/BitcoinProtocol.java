public class BitcoinProtocol {
    public static void handleIncomingMessage(byte[] incomingMessageBytes, MessageType expectedMessage){
        // handle incoming message
        // will tell whether the incoming message is valid or not
        // will give out appropriate reply for the message
    }
}
